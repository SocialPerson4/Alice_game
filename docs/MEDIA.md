# 媒体说明

公开仓库只包含核心代码、SQL、测试和说明文档，不包含图片、音乐、音效、M4A、MP4 或 AVI 文件。

这不代表相关功能没有实现：前端仍保留图片场景、背景音乐切换、音量状态、视频场景和 Web Audio 合成音效的代码。没有本地媒体时，页面会出现空白背景或跳过缺失资源，但剧情逻辑及后端接口仍可审阅。

完整媒体仅保存在课程项目原始归档中。如后续决定公开，可再通过独立发行包或 Git LFS 发布，避免让主仓库被大文件占用。
